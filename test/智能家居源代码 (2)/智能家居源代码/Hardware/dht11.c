#include "dht11.h"

#include "stm32f4xx_conf.h"


uint8_t dht11_data[5]={0x00,0x00,0x00,0x00,0x00};
uint8_t sum = 0;

#define DHT11_Pin  GPIO_Pin_1

/*
*????????
*/
void DHT11_GPIO_OUT(void)
{
	GPIO_InitTypeDef  GPIO_InitStructure;
	
	RCC_AHB1PeriphClockCmd(RCC_AHB1Periph_GPIOB ,ENABLE); 
		
  	GPIO_InitStructure.GPIO_Pin = DHT11_Pin;
 	GPIO_InitStructure.GPIO_Mode = GPIO_Mode_OUT; 
	GPIO_InitStructure.GPIO_OType = GPIO_OType_PP;
 	GPIO_InitStructure.GPIO_Speed = GPIO_Speed_50MHz;
  	GPIO_Init(GPIOB, &GPIO_InitStructure);
}

/*
*?????????
*/
void DHT11_GPIO_IN(void)
{
	GPIO_InitTypeDef  GPIO_InitStructure;
	
	RCC_AHB1PeriphClockCmd(RCC_AHB1Periph_GPIOB ,ENABLE);
		
  	GPIO_InitStructure.GPIO_Pin = DHT11_Pin;	
  	GPIO_InitStructure.GPIO_Mode = GPIO_Mode_IN;
	GPIO_InitStructure.GPIO_PuPd = GPIO_PuPd_UP;
  	GPIO_Init(GPIOB, &GPIO_InitStructure);
}

/*
*?????????
*/
uint8_t DHT_Read_Byte(void)
{
	uint8_t temp;
	uint8_t ReadDat = 0;
	
	uint8_t retry = 0;
	uint8_t i;
	for(i=0; i<8;i++)
	{	
		//????¦Ë?????????????????????50us
		while(GPIO_ReadInputDataBit(GPIOB, DHT11_Pin) == 0 && retry< 100)
		{
			Delay_us(1);
			retry++;
		}
		retry = 0;
		Delay_us(30);
		temp = 0;
		//???????????16us-28us??????????0??????????70us??????????1??
		if(GPIO_ReadInputDataBit(GPIOB, DHT11_Pin)==1) temp=1;
		while(GPIO_ReadInputDataBit(GPIOB, DHT11_Pin) == 1 && retry<100)
		{
			Delay_us(1);
			retry++;
		}
		retry = 0;
		ReadDat<<=1;
		ReadDat|=temp;
	}
	return ReadDat;
}

/*
*??????????
*/
uint8_t DHT_Read(void)
{
	uint8_t i;
	uint8_t retry = 0;
	//??????????????????18ms
	DHT11_GPIO_OUT();
	GPIO_ResetBits(GPIOB, DHT11_Pin);
	Delay_ms(18);
	//??????????40ms
	GPIO_SetBits(GPIOB, DHT11_Pin);
	Delay_us(40);
	//?????????????
	DHT11_GPIO_IN();
	Delay_us(20);
	//??????????????
	if(GPIO_ReadInputDataBit(GPIOB, DHT11_Pin) == 0)
	{	
		//??????????????????????100us
		while(GPIO_ReadInputDataBit(GPIOB, DHT11_Pin)==0&&retry<100)
		{
			Delay_us(1);
			retry++;
		}
		retry=0;
		//?????????????????100us
		while(GPIO_ReadInputDataBit(GPIOB, DHT11_Pin)==1&&retry<100)
		{
			Delay_us(1);
			retry++;
		}
		retry=0;
		//???????????dat[0]?????????????dat[1]????§³???????dat[2]?????????????dat[3]????§³???????dat[4]?§µ???????
		for(i=0;i<5;i++)
		{
			dht11_data[i] = DHT_Read_Byte();
		}
		Delay_us(50);
	}
	//??§µ???????=???????????+???§³??????+???????????+???§³??????????????????
	sum = dht11_data[0] + dht11_data[1] + dht11_data[2] + dht11_data[3];
	if(dht11_data[4]  == sum)
	{
		return 1;
	}
	else
		return 0;
}


//if(flag)
//{	
//	if( DHT_Read() )//??????????????????????????
//	{
//		flag=0;//???¦Ë???????????¦Ì??
//		OLED_ShowNum(1,1,dht11_data[2],2);		//?????????
//		OLED_ShowChar(1,3,'.');
//		OLED_ShowNum(1,4,dht11_data[3],1);		//?????????
//		
//		OLED_ShowNum(2,1,dht11_data[0],2);		//?????????
//		OLED_ShowChar(2,3,'.');
//		OLED_ShowNum(2,4,dht11_data[1],1);		//?????????				
//	}
//}


